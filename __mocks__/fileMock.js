/**
 * Mock for static file imports in Jest tests
 * 
 * This mock handles imports of image files, fonts, and other static assets
 * that would normally be processed by webpack in the Next.js build.
 */

module.exports = 'test-file-stub'